name: Pusula Pages CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  check-html:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Console files exist
        run: |
          test -f index.html
          test -f ops/index.html
          test -f about/index.html
          test -f pusula-serve/index.html
          test -f legal/index.html
          grep -q 'function pack(' index.html
          grep -q 'function paint(' ops/index.html
          grep -q 'vram:32' index.html
          grep -q 'id="boot"' index.html

      # Original Go job kept as comments — this repo has no Go module:
      # - uses: actions/setup-go@v5
      #   with:
      #     go-version: '1.22'
      # - run: go test -v -race ./...
      # - run: go build -v -o bin/pusula-serve cmd/main.go
